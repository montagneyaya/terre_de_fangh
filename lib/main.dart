/// Main entry point for the Terre de Fangh application.
/// 
/// This is the starting point of the Flutter application. It initializes and runs
/// the [MyApp] widget which serves as the root of the application.

import 'package:flutter/material.dart';
import 'package:myapp/src/app.dart';

/// The entry point of the application.
/// 
/// Initializes the Flutter framework and runs the [MyApp] widget.
void main() {
  runApp(const MyApp());
}
